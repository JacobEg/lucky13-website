/**
 * Fetches games data.
 * @param {*} repoOwner 
 * @param {*} repoName 
 * @param {*} personalAccessToken 
 * @returns 
 */
export async function getGames(repoOwner, repoName, personalAccessToken) {
    return {games: []};
}